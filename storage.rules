rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read access to all files for the TV Player
      // In production, you might want to restrict this to a specific folder
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
